def parse_mxml(filename):
    """
    Takes in a filename and returns a list containing (Note, int) tuples, where the first element is a particular note (pitch and duration), and the second element is the onset, in multiples of the divisions of the particular part
    """
